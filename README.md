# Dropdown Menu Component (тестовое задание)

**Dropdown Menu** - современный React компонент выпадающего меню с автоматическим позиционированием, разработанный на TypeScript с полным покрытием тестами.

## 🌐 Публикация
- Проект опубликован на GitHub Pages и доступен по ссылке: [открыть проект](https://hazar84.github.io/dropdown-menu-app)

## 🎯 Функциональность

### Основные возможности
- **🎯 Автоматическое позиционирование** - меню автоматически открывается в сторону с наибольшим свободным пространством
- **📍 Поддерживаемые позиции**: вниз-вправо, вверх-вправо, вниз-влево, вверх-влево
- **🔒 Единовременное открытие** - только одно меню может быть активно в любой момент
- **📱 Адаптивность к скроллу** - меню пересчитывает позицию при скролле страницы
- **🎨 Кастомный триггер** - поддержка любого React элемента в качестве триггера
- **🖱️ Интуитивное управление** - закрытие по клику снаружи или повторному клику на триггер

### Особенности реализации
- Автоматическое определение стороны открытия на основе доступного пространства
- Адаптация при скролле - меню перерисовывается при изменении видимости
- Закрытие при скрытии триггера - если триггер выходит из вьюпорта
- Обработка клавиатуры - поддержка Enter, Space и Escape клавиш
- Callback выполнение - вызов колбэка пункта меню с последующим закрытием

## 🛠 Технологический стек
- Frontend: React 18 + TypeScript
- Сборка: Vite
- Тестирование: Jest + React Testing Library
- Стилизация: CSS Modules
- Линтинг: ESLint + Prettier
- Деплой: GitHub Pages

## 📋 Системные требования
- Node.js 16+
- npm 8+ или yarn 1.22+
- Современный браузер с поддержкой ES6+

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/hazar84/dropdown-menu-app.git
cd dropdown-menu-app
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Запуск в режиме разработки
```bash
npm run dev
```
Приложение будет доступно по адресу: http://localhost:3000

### 4. Запуск тестов
```bash
npm test
```

### 5. Сборка для production
```bash
npm run build
```

### 6. Деплой на GitHub Pages
```bash
npm run deploy
```

## 📁 Архитектура проекта
```text
src/
├── components/
│   ├── DropdownMenu/          # Основной компонент дропдауна
│   │   ├── DropdownMenu.tsx
│   │   ├── DropdownMenu.module.css
│   │   ├── DropdownMenu.test.tsx
│   │   └── index.ts
│   └── Layout/                # Демонстрационный лейаут
│       ├── Layout.tsx
│       └── Layout.module.css
├── hooks/
│   └── useDropdown.ts         # Кастомный хук для управления состоянием
├── types/
│   └── dropdown.ts            # TypeScript типы
├── utils/
│   └── position.ts            # Утилиты для позиционирования
├── styles/
│   └── globals.css            # Глобальные стили
├── App.tsx                    # Корневой компонент
└── main.tsx                   # Точка входа
```

## 🎯 Ключевые особенности

### Умное позиционирование
```typescript
// Компонент автоматически определяет лучшую позицию
const calculatePosition = () => {
  // Расчет доступного пространства вокруг триггера
  // Выбор оптимального направления открытия
};
```

### Управление состоянием
```typescript
// Хук useDropdown предоставляет полный контроль
const {
  isOpen,
  position,
  triggerRef,
  contentRef,
  toggle,
  close
} = useDropdown();
```

### Типизация TypeScript
```typescript
interface DropdownMenuItem {
  id: string;
  label: string;
  onClick: () => void;
  icon?: string;
}

type Position = 'bottom-right' | 'top-right' | 'bottom-left' | 'top-left';
```

## 🎨 Демонстрация

### Примеры использования
```typescript
// Простое меню
<DropdownMenu
  trigger={<button>Открыть меню</button>}
  items={[
    {
      id: '1',
      label: 'Пункт меню 1',
      onClick: () => console.log('Пункт 1')
    },
    {
      id: '2', 
      label: 'Пункт меню 2',
      onClick: () => console.log('Пункт 2')
    }
  ]}
/>

// Меню с иконками
<DropdownMenu
  trigger={<IconButton />}
  items={menuItemsWithIcons}
/>
```

### Демонстрационные сценарии
- **Слева** - меню открывается вправо
- **По центру** - адаптивное позиционирование
- **Справа** - меню открывается влево
- **При скролле** - автоматическая перепозиция

## 🔧 Конфигурация

### Jest для тестирования
```javascript
// jest.config.js
module.exports = {
  testEnvironment: 'jsdom',
  moduleNameMapper: {
    '\\.(css|less|scss|sass)$': 'identity-obj-proxy',
    '^@/(.*)$': '<rootDir>/src/$1'
  }
};
```

### TypeScript с алиасами
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

## 🧪 Тестирование

### Покрытие тестами
- Рендеринг триггера
- Открытие/закрытие меню
- Обработка кликов по пунктам
- Обработка клавиатурных событий
- Моки зависимостей

### Пример теста
```typescript
it('opens and closes on trigger click', () => {
  render(<DropdownMenu trigger={<button>Menu</button>} items={items} />);
  
  const trigger = screen.getByText('Menu');
  fireEvent.click(trigger);
  expect(screen.getByText('Пункт меню')).toBeInTheDocument();
  
  fireEvent.click(trigger);
  expect(screen.queryByText('Пункт меню')).not.toBeInTheDocument();
});
```

## 📱 Адаптивность
- **Адаптивное позиционирование** - учитывает размеры viewport
- **Поддержка мобильных устройств** - touch-события
- **Доступность** - ARIA атрибуты и клавиатурная навигация

## 🎯 Результат разработки

### Проект разработан с использованием современных лучших практик фронтенд-разработки с акцентом на:
- **Чистую архитектуру** и модульность компонентов
- **Strict TypeScript** для надежности типов
- **Полное покрытие тестами** с Jest и React Testing Library
- **Автоматическое позиционирование** без сторонних библиотек
- **Доступность** и клавиатурную навигацию
- **Производительность** и оптимизацию перерисовок
- **Кастомизируемость** через props и CSS Modules
- **Профессиональную настройку** линтинга и форматирования

## Статус проекта: ✅ Завершено | 🧪 Протестировано | 🌐 Опубликовано